[default]

# ssh connection info
user = nb
host = nikolay.bg

# the path on the remote server, where to copy the shared files
remote-path = /var/www/nb/nikolay.bg/mess/

# the url template for your files, the filename will be appended in the end
# this url should correspond to the remote path above
url = http://nikolay.bg/mess/

# you can specify additional options for scp if needed, for example -P XX to use a different port
#scp-options = -P 1022
